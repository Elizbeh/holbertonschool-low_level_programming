#include "main.h"

/**
   * main - check the code.
    *
     * Return: Always 0.
      */
int main(void)
{
	char c;

	putchar(c);
	scanf("%c", &c);
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))

		_putchar(a);
	else
		putchar(b);
	return (0);
}
