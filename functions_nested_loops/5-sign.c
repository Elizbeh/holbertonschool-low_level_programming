#include "main.h"

/**
 * main - Write a program that prints _putchar, followed by a new line.
 *
 * Return: Always 0.
 */

int main(void)
{

	char c;

	_putchar(c);
	scanf("%c", &c);
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
		_putchar(b);
	else
		_putchar(d);
	return (0);
}

