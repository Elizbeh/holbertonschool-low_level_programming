#include "main.h"
#include<stdio.h>

/**
 * main - int _abs(int);
 *
 * Return: Always 0.
 */

int main(void)
{
	 int m = abs(200);
	 int n = abs(-400);

	    _putchar(abs);
	    _putchar(abs)

	    return (0);
}

